
<style>
    :root {
        --primary: #721a24;
        --secondary: #be2b3c;
        --success: #16a34a;
        --danger: #dc2626;
        --warning: #f59e0b;
        --dark: #0f172a;
        --gray: #64748b;
        --light: #f8fafc;
        --border: #e2e8f0;
    }

    body {
        background: #f1f5f9;
    }

    .dashboard-wrapper {
        padding: 30px 0;
    }

    .dashboard-header {
        background: linear-gradient(135deg, var(--primary), var(--secondary));
        border-radius: 24px;
        padding: 35px;
        color: white;
        position: relative;
        overflow: hidden;
        box-shadow: 0 20px 45px rgba(79, 70, 229, .18);
    }

        .dashboard-header::after {
            content: '';
            position: absolute;
            right: -80px;
            top: -80px;
            width: 250px;
            height: 250px;
            background: rgba(255,255,255,.08);
            border-radius: 50%;
        }

    .client-avatar {
        width: 110px;
        height: 110px;
        object-fit: cover;
        border-radius: 50%;
        border: 4px solid rgba(255,255,255,.25);
        box-shadow: 0 10px 25px rgba(0,0,0,.15);
    }

    .back-link {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        color: rgba(255,255,255,.9);
        text-decoration: none;
        margin-bottom: 18px;
        font-size: .95rem;
        transition: .3s ease;
    }

        .back-link:hover {
            color: white;
            transform: translateX(-3px);
        }

    .client-name {
        font-size: 2rem;
        font-weight: 700;
        margin-bottom: 6px;
    }

    .client-email {
        opacity: .85;
        font-size: .95rem;
    }

    .stats-grid {
        margin-top: 30px;
    }

    .dashboard-card {
        background: white;
        border-radius: 22px;
        padding: 24px;
        border: 1px solid var(--border);
        box-shadow: 0 10px 35px rgba(15, 23, 42, .05);
        transition: .3s ease;
        height: 100%;
        position: relative;
        overflow: hidden;
    }

        .dashboard-card:hover {
            transform: translateY(-6px);
            box-shadow: 0 18px 45px rgba(15, 23, 42, .08);
        }

        .dashboard-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 5px;
        }

    .articles-card::before {
        background: linear-gradient(to right, #721a24, #be2b3c);
    }

    .publications-card::before {
        background: linear-gradient(to right, #f59e0b, #facc15);
    }

    .videos-card::before {
        background: linear-gradient(to right, #dc2626, #fb7185);
    }

    .card-icon {
        width: 60px;
        height: 60px;
        border-radius: 18px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.4rem;
        color: white;
    }

    .icon-primary {
        background: linear-gradient(135deg, #721a24, #be2b3c);
    }

    .icon-warning {
        background: linear-gradient(135deg, #f59e0b, #facc15);
    }

    .icon-danger {
        background: linear-gradient(135deg, #dc2626, #fb7185);
    }

    .metric-title {
        color: var(--gray);
        font-size: .9rem;
        margin-bottom: 10px;
        font-weight: 500;
    }

    .metric-number {
        font-size: 2.2rem;
        font-weight: 800;
        color: var(--dark);
        margin-bottom: 12px;
    }

    .metric-details {
        background: #f8fafc;
        border-radius: 14px;
        padding: 14px;
        margin-top: 18px;
    }

    .metric-label {
        color: var(--gray);
        font-size: .85rem;
    }

    .metric-value {
        font-weight: 700;
        color: var(--success);
    }

    .dashboard-btn {
        margin-top: 20px;
        border-radius: 14px;
        padding: 12px 18px;
        font-weight: 600;
        border: none;
        transition: .3s ease;
    }

        .dashboard-btn:hover {
            transform: scale(1.02);
        }

    .overview-panel {
        margin-top: 28px;
        background: white;
        border-radius: 22px;
        padding: 24px;
        box-shadow: 0 10px 35px rgba(15, 23, 42, .05);
        border: 1px solid var(--border);
    }

    .overview-title {
        font-size: 1.25rem;
        font-weight: 700;
        color: var(--dark);
        margin-bottom: 20px;
    }

    .overview-item {
        padding: 16px;
        border-radius: 16px;
        background: #f8fafc;
        margin-bottom: 14px;
    }

        .overview-item h6 {
            margin-bottom: 8px;
            font-weight: 700;
            color: var(--dark);
        }

        .overview-item p {
            margin: 0;
            color: var(--gray);
        }

    
</style>