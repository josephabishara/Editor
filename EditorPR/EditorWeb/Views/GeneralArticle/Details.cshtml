@* ─── Details.cshtml ─────────────────────────────────────────────────────── *@
@model EditorViewModelLayer.GeneralArticleViewModel.GeneralArticleDTO
@{
    ViewData["Title"] = "Article Details";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h4><i class="bi bi-eye me-2"></i>Article Details</h4>
    <div class="d-flex gap-2">
        <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-outline-primary btn-sm">
            <i class="bi bi-pencil me-1"></i> Edit
        </a>
        <a asp-action="Index" class="btn btn-outline-secondary btn-sm">
            <i class="bi bi-arrow-left me-1"></i> Back
        </a>
    </div>
</div>

<div class="card shadow-sm">
    <div class="card-body">

        <div class="row g-3">
            <div class="col-md-3">
                <p class="text-muted mb-0 small">Date</p>
                <strong>@Model.Date.ToString("dd MMM yyyy")</strong>
            </div>
            <div class="col-md-3">
                <p class="text-muted mb-0 small">Website</p>
                <strong>@Model.WebsiteName</strong>
            </div>
            <div class="col-md-3">
                <p class="text-muted mb-0 small">Writer</p>
                <strong>@Model.WriterName</strong>
            </div>
            <div class="col-md-3">
                <p class="text-muted mb-0 small">Language</p>
                <strong>@(Model.Language ?? "—")</strong>
            </div>

            <div class="col-md-12">
                <p class="text-muted mb-0 small">Title</p>
                <strong>@Model.Title</strong>
            </div>

            @if (!string.IsNullOrEmpty(Model.ArticleURL))
            {
                <div class="col-md-12">
                    <p class="text-muted mb-0 small">Article URL</p>
                    <a href="@Model.ArticleURL" target="_blank"> Open Link </a>
                </div>
            }

            <div class="col-md-3">
                <p class="text-muted mb-0 small">Article Branding</p>
                <span class="badge bg-secondary">@(Model.ArticleBranding ?? "—")</span>
            </div>
            <div class="col-md-3">
                <p class="text-muted mb-0 small">Headline Branding</p>
                <span class="badge bg-secondary">@(Model.HeadlineBranding ?? "—")</span>
            </div>
            <div class="col-md-3">
                <p class="text-muted mb-0 small">Picture in Article</p>
                <strong>@(Model.PictureinArticle ?? "—")</strong>
            </div>
            <div class="col-md-3">
                <p class="text-muted mb-0 small">AI Generated</p>
                @if (Model.Generation == true)
                {
                    <span class="badge bg-info text-dark">Yes</span>
                }
                else
                {
                    <span class="badge bg-light text-dark border">No</span>
                }
            </div>

            @if (!string.IsNullOrEmpty(Model.Content))
            {
                <div class="col-md-12">
                    <p class="text-muted mb-1 small">Content</p>
                    <div class="border rounded p-3 bg-light">
                        @Html.Raw(Model.Content)
                    </div>
                </div>
            }
        </div>

    </div>
</div>
